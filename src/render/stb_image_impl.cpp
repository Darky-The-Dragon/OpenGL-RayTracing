// Enable stb_image implementation in this translation unit.
// This must appear in exactly one .cpp file in the entire project.
#define STB_IMAGE_IMPLEMENTATION

#include "stb_image.h"  // stb_image loader (PNG, JPG, HDR, etc.)
